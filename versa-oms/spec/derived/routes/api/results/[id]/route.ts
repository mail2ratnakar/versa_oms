// GENERATED by gen_routes (Robot 6) — DO NOT EDIT.
import { getResults, updateResults, deleteResults } from "@/services/results.service";
import { ok, err } from "@/runtime/envelope";

export async function GET(_req: Request, { params }: { params: { id: string } }) {
  const row = await getResults(params.id);
  return row ? ok(row) : err("NOT_FOUND", 404, "results not found");
}

export async function PATCH(req: Request, { params }: { params: { id: string } }) {
  return ok(await updateResults(params.id, await req.json()));
}

export async function DELETE(_req: Request, { params }: { params: { id: string } }) {
  return ok(await deleteResults(params.id));
}
