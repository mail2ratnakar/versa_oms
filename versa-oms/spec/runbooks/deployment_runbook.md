# Deployment and Operations Runbook

## 20.001 — env_directus_url

DIRECTUS_URL points to Directus Cloud project, e.g. https://versaolympiads.directus.app.

**Build instruction:** Add deployment/runbook instruction for env_directus_url: DIRECTUS_URL points to Directus Cloud project, e.g. https://versaolympiads.directus.app.

**Check:** Deployment checklist verifies env_directus_url.

## 20.002 — env_directus_token

DIRECTUS_SERVICE_TOKEN stored server-side only; never in browser or committed files.

**Build instruction:** Add deployment/runbook instruction for env_directus_token: DIRECTUS_SERVICE_TOKEN stored server-side only; never in browser or committed files.

**Check:** Deployment checklist verifies env_directus_token.

## 20.003 — env_app_url

NEXT_PUBLIC_APP_URL contains public portal URL; safe to expose.

**Build instruction:** Add deployment/runbook instruction for env_app_url: NEXT_PUBLIC_APP_URL contains public portal URL; safe to expose.

**Check:** Deployment checklist verifies env_app_url.

## 20.004 — env_session_secret

SESSION_SECRET or auth secret required for secure sessions; server-side only.

**Build instruction:** Add deployment/runbook instruction for env_session_secret: SESSION_SECRET or auth secret required for secure sessions; server-side only.

**Check:** Deployment checklist verifies env_session_secret.

## 20.005 — env_razorpay

RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET and RAZORPAY_WEBHOOK_SECRET stored server-side only when payment enabled.

**Build instruction:** Add deployment/runbook instruction for env_razorpay: RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET and RAZORPAY_WEBHOOK_SECRET stored server-side only when payment enabled.

**Check:** Deployment checklist verifies env_razorpay.

## 20.006 — env_email

EMAIL provider credentials stored server-side only when notifications enabled.

**Build instruction:** Add deployment/runbook instruction for env_email: EMAIL provider credentials stored server-side only when notifications enabled.

**Check:** Deployment checklist verifies env_email.

## 20.007 — env_pdf_provider

PDF provider endpoint/key stored server-side only when certificate PDF service enabled.

**Build instruction:** Add deployment/runbook instruction for env_pdf_provider: PDF provider endpoint/key stored server-side only when certificate PDF service enabled.

**Check:** Deployment checklist verifies env_pdf_provider.

## 20.008 — local_setup

Install Node LTS, package manager, create .env.local from .env.example, run install and dev server.

**Build instruction:** Add deployment/runbook instruction for local_setup: Install Node LTS, package manager, create .env.local from .env.example, run install and dev server.

**Check:** Deployment checklist verifies local_setup.

## 20.009 — directus_setup

Use Directus Cloud for backend; configure collections, roles, permissions, files and optional flows via MCP/admin.

**Build instruction:** Add deployment/runbook instruction for directus_setup: Use Directus Cloud for backend; configure collections, roles, permissions, files and optional flows via MCP/admin.

**Check:** Deployment checklist verifies directus_setup.

## 20.010 — hostinger_setup

Deploy Next.js app to Hostinger VPS or equivalent Node deployment; configure reverse proxy, SSL, env vars and process manager.

**Build instruction:** Add deployment/runbook instruction for hostinger_setup: Deploy Next.js app to Hostinger VPS or equivalent Node deployment; configure reverse proxy, SSL, env vars and process manager.

**Check:** Deployment checklist verifies hostinger_setup.

## 20.011 — build_command

Use npm/pnpm install, lint, typecheck and production build command generated by project scaffold.

**Build instruction:** Add deployment/runbook instruction for build_command: Use npm/pnpm install, lint, typecheck and production build command generated by project scaffold.

**Check:** Deployment checklist verifies build_command.

## 20.012 — start_command

Use production start or standalone output with process manager depending on hosting method.

**Build instruction:** Add deployment/runbook instruction for start_command: Use production start or standalone output with process manager depending on hosting method.

**Check:** Deployment checklist verifies start_command.

## 20.013 — ssl_domain

Enable HTTPS for portal domain; restrict CORS/origins to approved domains only.

**Build instruction:** Add deployment/runbook instruction for ssl_domain: Enable HTTPS for portal domain; restrict CORS/origins to approved domains only.

**Check:** Deployment checklist verifies ssl_domain.

## 20.014 — database_backup

Use Directus Cloud backup/export plan where available; document manual export fallback for schema/data/files.

**Build instruction:** Add deployment/runbook instruction for database_backup: Use Directus Cloud backup/export plan where available; document manual export fallback for schema/data/files.

**Check:** Deployment checklist verifies database_backup.

## 20.015 — file_backup

Ensure Directus files are backed up/exportable or covered by provider backup policy.

**Build instruction:** Add deployment/runbook instruction for file_backup: Ensure Directus files are backed up/exportable or covered by provider backup policy.

**Check:** Deployment checklist verifies file_backup.

## 20.016 — restore_drill

Before go-live, document how to restore schema/data/files into staging and verify a sample school journey.

**Build instruction:** Add deployment/runbook instruction for restore_drill: Before go-live, document how to restore schema/data/files into staging and verify a sample school journey.

**Check:** Deployment checklist verifies restore_drill.

## 20.017 — monitoring_logs

Track app logs, API errors, failed webhooks, failed logins, file access errors and job failures.

**Build instruction:** Add deployment/runbook instruction for monitoring_logs: Track app logs, API errors, failed webhooks, failed logins, file access errors and job failures.

**Check:** Deployment checklist verifies monitoring_logs.

## 20.018 — webhook_monitoring

Monitor Razorpay webhook failures, duplicates, amount mismatches and signature failures.

**Build instruction:** Add deployment/runbook instruction for webhook_monitoring: Monitor Razorpay webhook failures, duplicates, amount mismatches and signature failures.

**Check:** Deployment checklist verifies webhook_monitoring.

## 20.019 — email_monitoring

Monitor bounces, failures and suppressed recipients for transactional mail.

**Build instruction:** Add deployment/runbook instruction for email_monitoring: Monitor bounces, failures and suppressed recipients for transactional mail.

**Check:** Deployment checklist verifies email_monitoring.

## 20.020 — release_checklist

Before release, run build, tests, permission checks, public route checks, payment mock/sandbox test and backup check.

**Build instruction:** Add deployment/runbook instruction for release_checklist: Before release, run build, tests, permission checks, public route checks, payment mock/sandbox test and backup check.

**Check:** Deployment checklist verifies release_checklist.

## 20.021 — rollback_plan

Keep previous deployment package/commit; rollback frontend independently of Directus schema when possible; schema migrations need rollback notes.

**Build instruction:** Add deployment/runbook instruction for rollback_plan: Keep previous deployment package/commit; rollback frontend independently of Directus schema when possible; schema migrations need rollback notes.

**Check:** Deployment checklist verifies rollback_plan.

## 20.022 — schema_migration_policy

Every schema change requires migration note, impact analysis, backup/checkpoint and rollback approach.

**Build instruction:** Add deployment/runbook instruction for schema_migration_policy: Every schema change requires migration note, impact analysis, backup/checkpoint and rollback approach.

**Check:** Deployment checklist verifies schema_migration_policy.

## 20.023 — production_seed_data

Seed only required roles, statuses, first olympiad, exam slots and templates; no fake student data in production.

**Build instruction:** Add deployment/runbook instruction for production_seed_data: Seed only required roles, statuses, first olympiad, exam slots and templates; no fake student data in production.

**Check:** Deployment checklist verifies production_seed_data.

## 20.024 — staging_seed_data

Use anonymised/fake schools and students for tests; never real child/parent data unless approved.

**Build instruction:** Add deployment/runbook instruction for staging_seed_data: Use anonymised/fake schools and students for tests; never real child/parent data unless approved.

**Check:** Deployment checklist verifies staging_seed_data.

## 20.025 — admin_accounts

Create primary admin and emergency admin; enable MFA where possible; do not share accounts.

**Build instruction:** Add deployment/runbook instruction for admin_accounts: Create primary admin and emergency admin; enable MFA where possible; do not share accounts.

**Check:** Deployment checklist verifies admin_accounts.

## 20.026 — mcp_access_window

Enable Directus MCP builder access only during build/change windows; use dedicated builder user; disable/limit after work.

**Build instruction:** Add deployment/runbook instruction for mcp_access_window: Enable Directus MCP builder access only during build/change windows; use dedicated builder user; disable/limit after work.

**Check:** Deployment checklist verifies mcp_access_window.

## 20.027 — go_live_gate

Go live only after business owner signs off MVP flow, finance signs payment flow, operations signs exam/courier flow, and security checks pass.

**Build instruction:** Add deployment/runbook instruction for go_live_gate: Go live only after business owner signs off MVP flow, finance signs payment flow, operations signs exam/courier flow, and security checks pass.

**Check:** Deployment checklist verifies go_live_gate.

## 20.028 — post_launch_daily_checks

Daily check registrations, payment mismatches, failed uploads, courier exceptions, result approval queues, failed notifications and security alerts.

**Build instruction:** Add deployment/runbook instruction for post_launch_daily_checks: Daily check registrations, payment mismatches, failed uploads, courier exceptions, result approval queues, failed notifications and security alerts.

**Check:** Deployment checklist verifies post_launch_daily_checks.

## 20.029 — incident_response

Define incident owner, severity levels, containment steps, school notification decision and audit export process.

**Build instruction:** Add deployment/runbook instruction for incident_response: Define incident owner, severity levels, containment steps, school notification decision and audit export process.

**Check:** Deployment checklist verifies incident_response.

## 20.030 — support_process

Create support workflow for school login issues, upload failures, payment mismatch, slot change, courier exception and certificate correction.

**Build instruction:** Add deployment/runbook instruction for support_process: Create support workflow for school login issues, upload failures, payment mismatch, slot change, courier exception and certificate correction.

**Check:** Deployment checklist verifies support_process.

## 20.031 — data_retention_review

Before production, confirm retention periods for student data, upload files, OMR files, certificates and audit logs.

**Build instruction:** Add deployment/runbook instruction for data_retention_review: Before production, confirm retention periods for student data, upload files, OMR files, certificates and audit logs.

**Check:** Deployment checklist verifies data_retention_review.

## 20.032 — final_production_report

Produce build/deployment report with URLs, versions, env checklist, test result summary, open risks and manual decisions.

**Build instruction:** Add deployment/runbook instruction for final_production_report: Produce build/deployment report with URLs, versions, env checklist, test result summary, open risks and manual decisions.

**Check:** Deployment checklist verifies final_production_report.

## 20.033 — gl_build_passes

Production build passes without TypeScript/lint/build errors.

**Build instruction:** Add go-live checklist item: Production build passes without TypeScript/lint/build errors.

**Check:** Confirm checklist item gl_build_passes before production launch.

## 20.034 — gl_tests_pass

All MVP unit/API/permission/E2E tests pass or exceptions are documented.

**Build instruction:** Add go-live checklist item: All MVP unit/API/permission/E2E tests pass or exceptions are documented.

**Check:** Confirm checklist item gl_tests_pass before production launch.

## 20.035 — gl_public_private

Public user cannot access private collections, files or school data.

**Build instruction:** Add go-live checklist item: Public user cannot access private collections, files or school data.

**Check:** Confirm checklist item gl_public_private before production launch.

## 20.036 — gl_school_isolation

Two-school test proves cross-school data isolation.

**Build instruction:** Add go-live checklist item: Two-school test proves cross-school data isolation.

**Check:** Confirm checklist item gl_school_isolation before production launch.

## 20.037 — gl_file_access

Exam materials and certificates are private and audited on download.

**Build instruction:** Add go-live checklist item: Exam materials and certificates are private and audited on download.

**Check:** Confirm checklist item gl_file_access before production launch.

## 20.038 — gl_payment_sandbox

Payment link/webhook flow verified in sandbox/mock with signature/idempotency.

**Build instruction:** Add go-live checklist item: Payment link/webhook flow verified in sandbox/mock with signature/idempotency.

**Check:** Confirm checklist item gl_payment_sandbox before production launch.

## 20.039 — gl_manual_payment

Manual payment confirmation and reversal tested with evidence/audit.

**Build instruction:** Add go-live checklist item: Manual payment confirmation and reversal tested with evidence/audit.

**Check:** Confirm checklist item gl_manual_payment before production launch.

## 20.040 — gl_result_approval

Results cannot publish without approval and cannot auto-publish after OMR import.

**Build instruction:** Add go-live checklist item: Results cannot publish without approval and cannot auto-publish after OMR import.

**Check:** Confirm checklist item gl_result_approval before production launch.

## 20.041 — gl_certificate_verify

Public certificate verification exposes only whitelisted metadata.

**Build instruction:** Add go-live checklist item: Public certificate verification exposes only whitelisted metadata.

**Check:** Confirm checklist item gl_certificate_verify before production launch.

## 20.042 — gl_backup_ready

Backup/export and restore plan documented and checked.

**Build instruction:** Add go-live checklist item: Backup/export and restore plan documented and checked.

**Check:** Confirm checklist item gl_backup_ready before production launch.

## 20.043 — gl_admin_accounts

Admin accounts/MFA/emergency admin verified.

**Build instruction:** Add go-live checklist item: Admin accounts/MFA/emergency admin verified.

**Check:** Confirm checklist item gl_admin_accounts before production launch.

## 20.044 — gl_env_safe

Production environment variables set and no real secrets committed.

**Build instruction:** Add go-live checklist item: Production environment variables set and no real secrets committed.

**Check:** Confirm checklist item gl_env_safe before production launch.

## 20.045 — gl_business_signoff

Business owner approves flow and assumptions.

**Build instruction:** Add go-live checklist item: Business owner approves flow and assumptions.

**Check:** Confirm checklist item gl_business_signoff before production launch.

## 20.046 — gl_finance_signoff

Finance approves payment/commission/reconciliation flow.

**Build instruction:** Add go-live checklist item: Finance approves payment/commission/reconciliation flow.

**Check:** Confirm checklist item gl_finance_signoff before production launch.

## 20.047 — gl_operations_signoff

Operations approves exam materials, courier and result process.

**Build instruction:** Add go-live checklist item: Operations approves exam materials, courier and result process.

**Check:** Confirm checklist item gl_operations_signoff before production launch.

## 20.048 — gl_security_signoff

Security/privacy checklist reviewed and approved.

**Build instruction:** Add go-live checklist item: Security/privacy checklist reviewed and approved.

**Check:** Confirm checklist item gl_security_signoff before production launch.
