// GENERATED by _validation/gen_modules.py — DO NOT EDIT.
import { NextRequest, NextResponse } from "next/server";
import { requireStaffScope } from "@/server/guards/requireStaffScope";
import { ok, meta } from "@/server/http/envelope";
import { listChildRecords } from "@/server/lib/listChild";

export async function GET(request: NextRequest, ctx: { params: Promise<{ id: string }> }) {
  const guard = await requireStaffScope(request, "notification_ops", "read");
  if (!guard.ok) return NextResponse.json(guard.body, { status: guard.status });
  const { id } = await ctx.params;
  const data = await listChildRecords("notification_deliveries", "template_id", id, guard.actor);
  return NextResponse.json(ok(data, meta(guard.requestId, "notification_ops")));
}
