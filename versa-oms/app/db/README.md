# Database Folder

This folder will hold the implementation database registry, enum registry and generated schema references.

The LLM must use `implementation/DATABASE_SCHEMA_REGISTRY.json` as the canonical schema source.
