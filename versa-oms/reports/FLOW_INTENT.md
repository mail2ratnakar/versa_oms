# Flow intent — projected from annotated_flows.json

_Source: `source-of-truth/v2_supplement/annotated_flows.json` (the founder's /annotate output). Selectors are auto-resolved to compose fields/actions where unambiguous; **unresolved** steps need a human read. Implement compose/tabs/rules from this — the intent is in source, not chat._

_No annotated flows captured yet. Use **Send to source** on /annotate._
