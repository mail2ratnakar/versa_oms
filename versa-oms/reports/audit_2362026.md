# Versa Olympiads — Production-Readiness Self-Audit
Date: 2026-06-23 · Auditor prompt: 244-point evidence-only verification
App: `versa-oms/app` (Next.js 15.5.19 + React 19 + Supabase target)

---

## VERDICT

- **Status: NOT production-ready. SCAFFOLD-COMPLETE + FUNCTIONALLY-COMPLETE (local/dev mode).**
- **Production-grade readiness score: ~35–40 / 100.**
  - Architecture & spec-fidelity: ~9/10
  - Functional logic (local mode): ~8/10
  - Production-readiness (infra/auth/DB/CI/observability/security-hardening): ~3.5/10
- **Real and TESTED:** 131 real per-module tables (NO generic `business_entities`), server-side RBAC engine,
  field masking, append-only audit writer, idempotency, dual-approval (maker≠checker), gated status transitions,
  worker-job runner (retry/DLQ/idempotency).
- **NOT real:** live database, real auth, storage/signed URLs, CI, E2E/security/privacy test suites,
  observability, seed data, several security-hardening features.

---

## COMMANDS RUN (this audit) + RESULTS

| Command | Result |
|---|---|
| `git rev-parse --is-inside-work-tree` | **NOT a git repository** |
| `ls supabase/migrations` | 0001–0005 present |
| `npx next lint` | **PASS** (1 warning: custom-font in layout) |
| `npx tsc --noEmit` | **exit 0** |
| `npx vitest run` | **34/34 PASS** (6 files) |
| `npx next build --no-lint` | **Compiled successfully**, 83 routes generated |
| `pglast` parse 0001–0005 | all parse (grammar-valid; NOT applied to a live DB) |
| `python _validation/trace_audit.py` | 35/35 surfaces PASS, 131/131 tables, 0 dangling FK |

### Test output
```
Test Files  6 passed (6) · Tests  34 passed (34)
foundation(14) jobs(4) modules(6) school(3) transitions(4) dual_approval(3)
Skipped/disabled: 0. E2E/security/privacy/contract suites: 0 (do not exist).
```
Runtime HTTP smoke (manual, not automated): `/api/health` 200; create→422 zod; missing idempotency→400;
transition no-reason→422; dual-approval→applied:false; core_results:generate→jobs_enqueued→drain→succeeded.

### Code counts
192 `route.ts` · 41 `page.tsx` · 35 `service.ts` · 6 test files · 131 canonical tables · 19 job types.

### Stub/placeholder scan
TODO=19, skeleton=23, demo=3, FIXME/placeholder/mock/fake/stub=0.
Most TODO/skeleton hits are in UNUSED legacy per-module files (`server/modules/*/{permissions,validators,audit}.ts`)
NOT imported by routes. Wired stubs: `server/files/signedUrl.ts` (returns `/api/internal/demo-signed-url`),
`app/api/verify/certificate/[…]/route.ts` (`verification_status:"demo"`).

---

## SECTION-BY-SECTION FINDINGS (A–U)

### A. Build completion
1. **No git repo** — no branch/commit. 2. Tree at `versa-oms/app`; no diff (no VCS). 3. Packs merged into
`versa-oms/` (PROVENANCE.json). 4. Read: questionnaire SoT, 30 module specs, FIELD_MASKING_MATRIX, HIGH_RISK_ACTIONS,
RLS_POLICY_MATRIX, access_matrix, JOB_REGISTRY, QUEUE_CONFIG, design.md. 5. Skeleton replaced with real kernel; stubs
left: signedUrl.ts, verify route, unused legacy per-module files. 6. See scan above. 7. list/create return real
envelopes but LOCAL ECHO fallback (no DB). 8. No demo business data; verify route is demo. 9. Job handlers real-shaped
but query empty DB; unimplemented job types use default no-op. 10. Unit-only tests, no skipped.

### B. Commands/tests
11–13. lint PASS(1 warn), tsc exit 0, vitest 34/34, build OK 83 routes. 14. Migration "check" = pglast parse only,
not applied. 15–19. API-contract/security/privacy/E2E suites: NOT run (don't exist); worker covered by jobs.test.ts.
20. PRODUCTION build.

### C. Database
21/22. Supabase target; connection = LOCAL PLACEHOLDER (env fallback 127.0.0.1:54321 + "local-…" keys). 23. 0001–0005.
24. NOT applied. 25/26. 131 REAL per-module tables; `business_entities`=False. 27. RLS defined (0003 force on 132 tables,
deny-by-default) + app guards; not active. 28. Hard delete: app pattern (archived_at + filters); NO DB trigger. 29.
archived_at/version/status: YES. 30. audit append-only (code) yes; DB constraint no. 31. idempotency_keys: table+code,
in-memory fallback. 32. feature_flags: NOT implemented. 33. file metadata tables: NOT implemented. 34. job_runs: YES.
35. public verification table: NOT implemented.

### D. Auth
36. NOT real — dev super_admin/school fallback when IS_LOCAL. 37–39. invite-only/no-self-reg/disabled-block: modeled,
not enforced. 40. staff vs school separate: YES. 41. school_id from session: YES (but dev session). 43/44/45. secure
cookies / hashed-expiring invite tokens / session revocation: NOT implemented.

### E. RBAC
46. 20 roles referenced, not seeded as managed table. 47/48/49. server-side role/scope, ignore browser role/scope/
school_id: YES (tested). 50/51. self-role-change block / last-super-admin protection: NOT implemented. 52/53. module +
action perms: YES (tested). 54. own-school: YES. 55/56/57. finance-no-PII/support-summary/courier-no-mapping: via
masking, partial. 58/59. auditor read-only / raw-audit restricted: policy-level. 60. cross-school test: NONE.

### F. Masking
61. Implemented (config/masking.json + masking.ts). 62–68. parent_phone/email, payment_reference, provider_payload,
answer_key, raw_omr, internal_note: YES, TESTED (8 tests). 69/70. private paths / signed-url-not-logged: signedUrl STUB.
71. public verify minimal: demo stub. 72. masking tests: YES.

### G. Audit
73–80. createAuditEvent on create/update/transition/job-drain; high-risk/approval/transition audited w/ reason+prev→new:
YES (runtime audit_event_id). 81. append-only (code): YES. 82. event_hash: NO. 83. DB modify/delete prevention: NO.
84. no dedicated audit-immutability test.

### H. Idempotency
85. X-Idempotency-Key required (400 if missing): YES. 86/87/88. replay-safe / same-key-same-result / conflict path:
coded; conflict not unit-tested. 89/94. worker idempotent: YES (tested). 90/91/92. notif/cert/result job idempotency:
via per record+action key. 93. tests: jobs idempotency yes; API replay no.

### I. API contract
94. vs openapi.json: NOT verified. 95/96/97. staff(19+core10+jobs/overview)/school(6)/public(demo): present. 98–100.
standard envelopes + 422/403/401: YES (envelope.ts, runtime-verified). 101. pagination yes, filtering minimal. 102.
signed-url convention: STUB. 103/104. raw-table block / public-private allowlist: NOT wired.

### J. 19 modules
All 19 company modules: service + collection + [id] + actions route + UI page + spec policy + transitions = IMPLEMENTED
(functional, local). trace_audit 19/19 OK. Multi-table per-module workflows beyond primary table+transitions = PARTIAL.
No per-module dedicated tests.

### K. High-risk
124/127/128/129/131/133. dual-approval (maker≠checker) on role change/material release+revoke/answer-key/result publish/
cert reissue-revoke: ENFORCED for HIGH_RISK_ACTIONS dual flags (runtime-verified). 125/126/134/135/137. reason-gated +
audited: YES. 138. self-approval blocked: YES (tested). 130/132/136. OMR gate / published-correction-versioning /
verify-policy-approval: partial/not versioned. 139. high-risk tests: dual_approval(3)+transitions(4) pass.

### L. Exam material security
140–143/145/148/149. private storage / hidden paths / short-lived URLs / school-scoped / time-gate / block-before-release
/ unpaid-block: NOT implemented (storage + finance-gate absent; signedUrl stub). 144. download audit: framework only.
146/147. revoke/replace as transitions. 150. tests: NONE.

### M. Finance gate
151/152/153. server-calculated amounts NO; server-controlled status YES (transitions); ignore browser amount/status
partial. 154. manual confirm audited: YES. 156/157. finance→slot/material gate (unpaid block): SPEC exists, NOT enforced.
159. tests: NONE.

### N. Roster
160/161. import + required-field validation: zod. 162. forbidden fields (Aadhaar/passport/bank) REJECTED: **NOT**
(passthrough) — GAP. 163/164. server-side unique candidate IDs: NOT implemented. 165/166/167. lock (transition) /
post-lock block / corrections workflow: lock yes, post-lock-block NO. 168. finance-no-PII: via masking. 169. tests: NONE.

### O. Eval→Results→Certificates
170–184. answer-key versioning, OMR import/validate/scoring, score-batch approval, result generation, ranking snapshot,
publication approval, immutability, cert generate/QR/revoke/reissue, public verify rate-limit: mostly NOT implemented as
computation — represented as tables + status transitions + job triggers (results.generate_batch, certificate.generate)
real-shaped on empty DB. Scoring/ranking/QR algorithms NOT written. 185. flow tests: NONE.

### P. Worker queues
186. real backend: NO (in-process). 187. 11 queues: queue_ids carried, not separate backends. 188. 19 job types
REGISTERED: YES. 189/190/194. retry + DLQ + idempotency: YES, TESTED. 191. failed jobs visible: /api/staff/jobs + UI.
192/193. failure alerts/tasks, privacy-safe logs: NO. 195. tests: jobs.test.ts(4) + runtime drain succeeded.

### Q. Observability
196–204. structured logs/redaction/metrics/alerts/tracing/error-monitoring/audit-monitoring: NOT wired. 200. health:
ONLY `/api/health`; `/db /storage /workers /audit /providers /release` MISSING. 205. tests: NONE.

### R. UI
206/207/208. staff(all)/school/public pages: YES (41). 209. role-aware nav: partial (static). 210. unauthorized hidden
in UI: NO (server-blocked yes). 211/212. design-system + loading/empty/error states: YES (Finverse glass). 213/214.
mobile/a11y: basic responsive, no a11y pass. 215. 83 build routes.

### S. Seed
216–222. seed loaded: NO (no DB). Seed pack in versa-oms/seed, not applied. Scenarios not runnable.

### T. CI/CD
223–228/232. GitHub Actions / CI / staging deploy / prod manual approval / migration checks / rollback drills / outputs:
NONE (no git; deploy pack = docs only). 229. conservative flags in .env.example, not enforced. 230/231. secrets =
placeholders only; env files safe.

---

## REMAINING GAPS (production blockers)
1. No git repo / no CI / no deploy workflows.
2. No live database; migrations unapplied; data paths fall back to in-memory/echo.
3. No real auth — dev fallback; no cookies/invite-hashing/session-revocation.
4. Storage + signed URLs are stubs; exam-material privacy + finance/payment gate NOT enforced.
5. No automated security/privacy/E2E/API-contract tests; cross-school + forbidden-field rejection + per-module business tests missing.
6. Eval→result→certificate COMPUTATION (scoring, ranking, QR, immutability/versioning) not implemented.
7. Observability + health sub-endpoints + feature-flag/file-metadata/public-verify tables missing.
8. event_hash, last-super-admin protection, self-role-change block, DB-level append-only/no-hard-delete triggers, rate limiting — not implemented.
9. Unused legacy per-module stub files (skeleton/TODO) still in tree.

## RISK
HIGH if treated as production (unauthenticated dev fallback, unmasked storage, unenforced finance/material gate).
LOW as a demonstrable scaffold/staging-candidate for internal review.

## NEXT ACTION (ordered to reach production)
1. `git init` + commit + GitHub Actions CI (lint/typecheck/test/build).
2. Wire live Supabase: apply 0001–0005, seed, replace dev-actor with real Supabase auth (secure cookies, hashed+expiring
   invite tokens, session revocation), activate RLS.
3. Implement storage + real signed URLs; enforce exam-material privacy + finance/payment gate (block unpaid/pre-release).
4. Implement eval→results→certificate computation (scoring, ranking snapshot, immutability/versioning, QR, public-verify
   table + rate limit).
5. Add security/privacy/E2E/API-contract test suites incl. cross-school denial, forbidden-field rejection, last-super-admin,
   self-role-change-block; add event_hash + DB append-only/no-hard-delete constraints.
6. Wire observability (structured logs+redaction, metrics, alerts, tracing) + remaining /api/health/* endpoints.
7. Remove unused legacy stub files; finalize feature flags + file-metadata + public-verification tables.

---

## UPDATE 2026-06-23 (later) — Items 3–7 IMPLEMENTED

Items 1 (git/CI) and 2 (live Supabase + real auth) remain DEFERRED. Items 3–7 from "NEXT ACTION"
are now implemented, unit-tested, and runtime-verified in local mode.

### New evidence (commands re-run)
- `npx tsc --noEmit` → exit 0
- `npx vitest run` → **50/50 PASS across 10 suites**
  (foundation, jobs, modules, school, transitions, dual_approval, **computation(7), security(4), privacy(2), contract(3)**)
- `npx next build --no-lint` → Compiled successfully, **89 routes** (was 83)
- `pglast` parse → migrations **0001–0007** all valid

### #3 Storage / signed URLs / finance + material gate — DONE (logic), verify on live DB
- `server/files/signedUrl.ts` REWRITTEN: real Supabase Storage `createSignedUrl` from `file_metadata`;
  never returns a raw path (returns null when unavailable). TTL from env.
- `server/lib/financeGate.ts` `isPaymentCleared()` (safe-by-default DENY).
- `app/api/school/materials/route.ts` CUSTOM route: **unpaid → 403** + **only released/downloaded packages listed**.
  Runtime-verified: 403 "locked until payment is cleared".
- `0006_platform_tables.sql` adds `file_metadata` (private object paths) + RLS.

### #4 Eval → results → certificate computation — DONE (pure), TESTED
- `server/eval/scoring.ts` (negative marking), `ranking.ts` (competition ranking, ties),
  `certificate.ts` (verification codes + QR + minimal public projection), `immutability.ts`
  (published immutable + version bump). 7 unit tests in `computation.test.ts`.

### #5 Security/privacy + tests — DONE (logic), DB-trigger parts pending apply
- `server/security/pii.ts` forbidden fields (Aadhaar/passport/bank/PAN/SSN/…). Wired into kernel
  create+update. Runtime-verified: 422 on passport_number.
- `server/security/staffPolicy.ts` self-role-change block (wired, tested) + last-super-admin protection
  (DB-count, verify on live DB).
- Audit hash chain: `createAuditEvent` now writes `event_hash`+`prev_hash` (0006 adds columns).
- `0007_immutability.sql`: audit_events append-only trigger + no-hard-delete triggers on 22 business tables.
- New suites: `security.test.ts`(4), `privacy.test.ts`(2), `contract.test.ts`(3), plus masking in foundation.

### #6 Observability + health + tables — DONE
- `server/lib/logger.ts` structured + redaction (tested). `featureFlags.ts` (env+DB, conservative).
- Health sub-endpoints LIVE: `/api/health/{db,storage,workers,audit,providers,release}`
  (runtime-verified: db 503-unreachable, workers/release 200).
- `0006` adds `feature_flags`, `file_metadata`, `public_verification` (+RLS).

### #7 Remove stubs + real verify — DONE
- Deleted **76** unused legacy files (`server/modules/*/{permissions,validators,audit,README}`).
- `signedUrl.ts` real (above). `app/api/verify/certificate/[…]` REWRITTEN: real `public_verification`
  lookup + minimal whitelisted fields + per-process rate limit. Runtime-verified: not_found (no more "demo").

### Updated production-grade score: ~55–60 / 100
(up from 35–40). Logic/enforcement layer now substantially complete; remaining gap is INFRASTRUCTURE
(items 1–2) + LIVE verification of DB-dependent pieces.

### PENDING VERIFICATION (requires live Supabase + applied migrations; cannot confirm in local mode)
1. Apply migrations 0001–0007 to a real Postgres/Supabase; confirm all DDL + triggers execute.
2. **DB triggers (0007):** verify UPDATE/DELETE on `audit_events` is rejected (append-only) and DELETE on
   the 22 business tables is rejected (no-hard-delete) — currently only grammar-parsed.
3. **Finance gate positive path:** with real `payments` rows, confirm a PAID school CAN list released
   materials (today only the unpaid→403 deny path is verifiable).
4. **Signed URLs:** with a private storage bucket + `file_metadata` rows, confirm a real short-lived signed
   URL is returned and raw paths never leak; confirm download is school-scoped + audited.
5. **last-super-admin protection:** with ≥1 super_admin row, confirm demoting/disabling the last one is blocked
   (local mode skips the count check).
6. **Cross-school denial:** with seeded multi-school data, confirm school A cannot read school B records
   (getModuleRecord ownership + RLS) — needs DB; no local test.
7. **Audit hash chain:** confirm `event_hash`/`prev_hash` persist and form a verifiable chain across rows.
8. **public_verification:** seed a published certificate row; confirm `/verify` returns valid→minimal fields,
   revoked→"revoked", and rate-limit 429 under load.
9. **Eval→cert E2E:** wire scoring/ranking/certificate functions into the live results/certificate services
   over real data (functions are unit-tested in isolation; not yet invoked by the module services end-to-end).
10. **Health/db + /storage:** confirm 200 once Supabase is reachable (today correctly 503).
11. Still DEFERRED (items 1–2): git init + CI; real Supabase auth (cookies, hashed+expiring invite tokens,
    session revocation), seed load, activate RLS, deploy.

---
*Generated from reproducible commands. Source of truth for follow-up actions.*
